# V6 validation ledger

V6 retains Club's negative result against plan §4.2. It does not repair
A1/A5, change Club's privacy policy, or reinterpret its passing bounded
campaign as plan acceptance. The second-admission expectation remains an
executing, failing TODO.

## Integration input

The integration merges exact V4 candidate
`127af627529362c66e4944ce0418913f1f31f3ed` into V5 evidence head
`772a514a08c2fac72fe534c8490c8d33a3e1c2d1`. V4 already contains the
landed V3 `7bd311f` ancestry. No history is rebased.

The shared boundary retains:

- V3's declared reads of preceding model state by audience rules, frozen
  snapshots for those reads, and rollback if the audience rule throws;
- V4's actor-only audience for unauthorized application/clock attempts
  and the client policy requiring an authorized verdict before disclosure;
- V5's role-derived audiences, public commitments and grant history,
  ambient disclosure folding, declared Member-grant effects and the
  members-visible `dap.disclose` act;
- Club's capability-restricted disclosure recipients, combined with
  Booking's event-level disclosure authorization filter.

The integrated generator selects a disclosable position before selecting
its eligible recipient, preserving the V5 draw order. V4 selected the
recipient first. Sale and Booking generated streams therefore need fresh
measurement here; their earlier campaign counts are historical results,
not predictions of this integration. Model and manifest sources retain
their respective input identities. Binding identities incorporate V3's
audience-read contract, so they and event ids can differ from old V5 runs.

Historical corpus data and boundaries are unchanged. Reproduce all nine
original Sale run-6 failures and their original minimality under snapshot
`009b5226bd77b9f9d5e7ccad70b39867ef3d1a41`; reproduce all 99 Booking
run-7 failures under `7ffa50814ec558781feeeae09b36ff8f089c2fb4`. Current
corpus tests state the repaired-foundation result separately. Club's
baseline source and three recovered linked traces remain unchanged.

Typecheck passed after installing the committed lockfile's dependencies
with `npm ci --ignore-scripts`. At this integration boundary no campaign
had run. During read-only mutation
design, a subagent ran small in-memory checker probes before this input
snapshot; these exploratory checks are not a snapshotted measurement.
They found clean controls and mismatches for three audience mutations,
a hidden-revocation serving mutation and the three binding cases. The committed tests repeat
and record those findings at an immutable input snapshot before the full
validation suite.

The imported historical `corpus/booking/run2/budget.ts` has an extra blank
line at EOF. It is retained byte-for-byte from V4 alongside the corpus.
The whitespace check against V4 passes; a diff against the V5 parent
reports that inherited line. No new whitespace defect is introduced.


## Run 1 input: formal mutation and integration checks

Input snapshot: `1db4d6fca0b40fd910cb00ce86060bc00ddce15d`,
"spike V6: freeze mutations and binding checks".
The active V6 request is now `6a4a45205d6c39d388198bbe493b0e999d841a1d`;
the earlier integration commit keeps its original request provenance.

`test/v6.test.ts` defines these deterministic checks before the formal run:

- Sale: narrow `offer` to its actor, then publish `accept`. Alice must
  disagree with the oracle with `no_such_offer`.
- Booking: narrow `occupancy` to its actor, then publish `free`. Bob must
  disagree with the oracle with `no_such_occupancy`.
- Club: narrow `standing` to its actor, then let a lapsed committee
  member vote. Bob must judge it effective while the oracle says `lapsed`.
- Each mutant changes only one audience policy; it preserves all model
  functions/configuration and every other kind. The same trace with the
  original package must pass consistency, invariants and privacy checks.
- For each model, compose two distinct shared intents. An unrelated
  private attach must preserve both binding and activation identities,
  and the first intent must succeed. An openly visible relevant attach
  changes the binding; the second saved intent must be `stale_binding`
  for every reader, not paused. A fresh intent must succeed. All-frontier
  checks must remain clean.
- A separate serving mutation hides a spine revocation without changing
  any recorded event or header. The checker must find Bob's unauthorized
  attempt effective in his view; restoring compliant serving must restore
  consistency. This is an intentionally noncompliant serving contract,
  not a private revocation permitted by F0.

The suite will also run all current corpus/trace tests and all three full
200-seed campaigns. It retains Club's existing-Member TODO. Results and
any integration failures will be recorded separately, without rewriting
historical fixtures or claims.

## Run 1 result

Command: `npm --prefix spike test`, Node `v26.8.2`. Result: exit 1;
130 tests, 128 pass, 1 failure, 1 executing/failing TODO, about 123 seconds.
The complete output is `evidence/v6/run1.log`; `evidence/v6/run1.json`
records all seven V6 cases, exact identities, verdicts, readers/frontiers
and campaign coverage in machine-readable form.

All seven V6 cases pass:

| Case | Measured finding |
| --- | --- |
| Sale actor-only offer | 1 mismatch; Alice at 5 rejects with `no_such_offer`, oracle accepts |
| Booking actor-only occupancy | 1 mismatch; Bob at 4 rejects with `no_such_occupancy`, oracle accepts |
| Club actor-only standing | 2 mismatches; Bob at 7 accepts a vote, oracle rejects with `lapsed` |
| Sale binding locality | Private attach at 4 preserves intent at 5; relevant attach at 6 makes saved intent at 7 `stale_binding`; fresh intent at 8 succeeds |
| Booking binding locality | Corresponding positions 3, 4, 5, 6, 7; all judgments agree |
| Club binding locality | Corresponding positions 3, 4, 5, 6, 7; all judgments agree |
| Hidden spine revocation | 2 mismatches; Bob at 4 retains affordances and at 5 accepts an unauthorized offer; restoring compliant serving is clean |

Every mutation has a clean control. Every binding case has zero
all-frontier consistency, invariant and privacy violations; both readers
interpret rather than pause and judge the obsolete intent `stale_binding`.

All three 200-seed campaigns pass at this snapshot, with zero violations
of their frozen checks. They are fresh measurements under the merged
foundation and generator:

| Model | Entries | Coverage selected from the full JSON record |
| --- | ---: | --- |
| Sale | 11,047 | 195 offers, 20 effective accepts, 118 unbound inspection requests |
| Booking | 12,000 | 2,553 requests, 652 effective occupancies, 471 linked occupancies, 324 effective cancels, 1,630 ticks |
| Club | 11,939 | 629 applications, 1,575 votes (1,001 effective), 343 admits (126 effective), 514 standings, 246 generated grants |

The ordinary failure is the Club corpus's literal description assertion:
V3's audience-read contract changes the binding and application event id.
The recorded `unknown_application` mismatch still reproduces, but the
description's truncated hash differs. No product or model repair is
required. The historical corpus data remain byte-for-byte unchanged.

To verify that boundary, `node --test spike/test/club-corpus.test.ts`
was run at exact V5 `772a514a08c2fac72fe534c8490c8d33a3e1c2d1`:
1 test passes, all three literal descriptions match, all three traces
are seven-step deletion-minimal, and the repaired controls are clean.
Output: `evidence/v6/historical-club-772a514.log`.

The TODO still asserts the original §4.2 requirement and fails: Dana's
second admission at 18 is effective after her Member grant at 15.
This remains the retained negative result, not a consistency failure.

## Run 2 input: corpus boundary and minimized planted faults

Input snapshot: `742e64a06d06226b456f489eb53deacc1bc9f5db`,
"spike V6: record run 1 and freeze corpus checks".
No production, model or manifest code changes after run 1. The Club
corpus assertion now remaps only the linked application-id preview in
the exact recorded description, and independently verifies the full
application id in the vote and projection. It retains all deletion
checks and repaired-model checks.

The V6 tests now capture each planted-fault trace, verify exact linked
replay, shrink it, check every single deletion, and require a clean
unmutated replay. Their minimized traces will be committed with the
measured results. This includes the noncompliant revocation-serving
mutation; its minimum may retain only the affordance mismatch.

Run 2 will execute the integrated suite with the three campaign tests
excluded by name, preserving run 1's complete 600-seed measurement.
Only tests and evidence documentation changed, so repeating campaigns
would not measure a changed implementation. Typecheck passes before the
snapshot. Results will follow separately.

## Run 2 result

From `spike/`, run:

```sh
node --test --test-skip-pattern='case [57], the campaign:' 'test/**/*.test.ts'
```

Result: exit 0; 127 tests, 126 pass, zero ordinary failures and one
executing/failing TODO, about 12 seconds. The three campaign tests were
excluded by name (Node does not include these in its skipped count).
Run 1 remains the full campaign measurement of the unchanged implementation.
Output and machine-readable evidence are `evidence/v6/run2.log` and
`evidence/v6/run2.json`. All seven V6 findings match run 1 exactly.

The corrected Club corpus test passes for all three seeds. The historical
findings match after remapping only the linked application-id preview;
the full ids in the vote and projection agree. Each seven-step trace
remains deletion-minimal under the preserved baseline and clean under the
repaired package.

The V6 planted-fault traces are committed under `corpus/v6/run2/` with
the run's exact snapshot, package, manifest, nonce and linked steps.
Sale (4 steps), Booking (4) and Club (7) were already deletion-minimal.
The revocation-serving trace shrinks from 4 to 3 steps: invitation,
acceptance and revocation suffice for an affordance mismatch; the original
test separately requires the unauthorized-offer outcome at the next
position. Every single-step deletion removes the mismatch, and every
minimum has a clean unmutated control. These deliberate mutants remain
test fixtures; production code is not weakened.

V6 adds zero model fixes or kinds. It integrates prior foundation changes
and corrects one test's historical identity boundary. Sale's recorded
repair-budget failure, Booking's bounded result and Club's negative
plan-acceptance result are retained. The experiment records still have
their previously documented protocol and coverage limitations.

## Shared-foundation scope and material limitations

V4's unauthorized-application audience clamp applies across models. In
Club it narrows the readable sets of unauthorized `vote`, `admit` and
`standing` attempts from the declared members audience to the actor;
unauthorized private kinds also stay actor-only. Authorized attempts
retain their declared audiences. The V6 campaigns measure Club with
this changed foundation, even though its model and manifest bytes are
unchanged. V5's original measurements keep their earlier foundation
snapshot and must not be described as measurements of V6.

V4 was approved by checker report #1017,
`git:sha1:e15db5d98cd3510f3f20f06b3d0e1ec58379d2b1#git:sha1:1def477d5a5f2df385b71c0c94ce0e0532f9c8fb`,
for exact candidate `127af627529362c66e4944ce0418913f1f31f3ed`.
Its nonblocking limitations still apply here:

- Booking's F2 payload guard recognizes exact `booker`/`purpose` keys;
  renamed keys, case variations and string encodings are outside that guard.
- Revoked Bookers are absent from Booking's later actor guard.
- A principal holding `dap.disclose` can disclose a position they cannot
  themselves read. The fixture's client disclosure policy is essential;
  the foundation does not enforce that model-specific privacy restriction.
- The historical Booking run-2 budget file has the retained EOF blank
  line. V4's later clean working-diff checks do not establish a clean
  diff from main. V6 preserves that historical file byte-for-byte.

Club additionally retains A1/A5, partial-disclosure dependency mismatch,
extra-field acceptance, the revocation privacy-budget false positive and
its dependency on the declared effect executor. No scope-changing
repair or revised experiment is commissioned in this integration.

## Evidence follow-up after independent approval #1805

Request:
`git:sha1:e15db5d98cd3510f3f20f06b3d0e1ec58379d2b1#git:sha1:a98ddab9ab83b9f46627672792347f1d04986d17`.
The exact checker report
`git:sha1:e15db5d98cd3510f3f20f06b3d0e1ec58379d2b1#git:sha1:1998582d8f1d14adfeca07b170f392f517b0dbdb`
was inspected as builder. It independently approved historical V6 head
`05c98e2d778124648834b9b00e772a6ad270a870`; the main wrapper is
`19af1c8fb46cc91c61a2027647f04a6f305b5cdc`. Approval preserves the
negative result and does not accept Club's original policy. This follow-up
addresses its six nonblocking evidence/wording items and needs its own review.

The new source snapshot is `e627460daf788d6dbf6d3f8cbaa28f8f0fc0368b`,
"spike V6: freeze evidence follow-up".
It changes only tests, a retrospective corpus companion and documentation.
Production code, generator, model fixtures and frozen manifests are unchanged.
No 600-seed rerun is planned; the focused noncampaign suite and typecheck
will be measured at the committed input, with new outputs kept separately.

New executable evidence:

- A5 cases name Dana's invitation acceptance, a malformed ineffective
  `apply`, and a `standing_reason` as the application id. Each proceeds to
  two votes and admission. The independent manifest invariants must detect
  the unknown application; the original requirement that votes/admission
  reject and no Member grant follows remains an executing TODO. Complete
  linked traces and deletion-minimal invariant failures will be retained.
  These retrospective cases do not repair the policy or become original
  predeclarations. The old generator targets application ids and never
  exercises these non-application targets.
- `test/v6.test.ts` now reads all four unchanged `corpus/v6/run2` records,
  verifies their original identities and control-entry ids, replays their
  recorded findings, and checks every deletion and unmutated control.
  `corpus/v6/expected.json` adds retrospective expected outcomes without
  changing the historical records. Exact mutation construction stays in
  the defining test module, including the same arrow-function text.

Record corrections, appended rather than rewriting historical outputs:

- At `e0d83bf092f8b1d75036ebd5aab868aba69b0510`, three audience-case
  entries in `evidence/v6/run1.json` renamed nested `reason` to
  `model_reason` in the oracle/view verdict summaries. The values did not
  change. The clarification distinguishes per-model refusal reasons from
  the aggregate `ineffective` reason; the original log is unchanged.
- Earlier typecheck claims came from live commands, with no committed
  output. Checker #1805 independently reproduced typecheck success.
  The follow-up records new typecheck output; it cannot retroactively
  create evidence for the earlier commands.
- The integration-boundary statement "No new whitespace defect is
  introduced" preceded the saved logs. The final historical V6 diff
  includes six whitespace-only lines in raw `run1.log` and `run2.log`.
  The Booking run-2 budget EOF line is inherited from V4. Old logs and
  corpus bytes remain unchanged; clean checks of new edits are a
  separate claim from historical diff checks.
- The mutation checks are hand-built traces with affected readers and
  later dependent events, not campaigns over mutants and not a
  must-readable floor. Binding preservation means equality of values,
  not reuse of a cache. An attach relevant to a kind can impose a private
  audience ceiling excluding even a later actor (existing C3a behavior).
- REPORT corrects 273 stubs, Booking's landed status, the named goals,
  the bounded audience-widening verdict and #1805/main provenance.
  The decision quotes Hugh's complete recommended option and records
  A3/A4 completed at `772a514`; A1/A5 remain negative.

### Follow-up 1 result

At the source snapshot above, the noncampaign command recorded earlier
passes: 133 tests, 129 pass, zero ordinary failures, and four executing,
failing TODOs (original A1 plus three A5 cases), exit 0, about 12 seconds.
The three unchanged campaign tests were excluded by name. Typecheck
also exits 0. Outputs are `evidence/v6/followup1.log` and
`evidence/v6/followup1-typecheck.log`; `followup1.json` records commands,
results, mutation identities, A5 outcomes and corpus checks.

The new test log removes trailing spaces/tabs from each line only, without
removing lines. Its raw and normalized SHA-256 digests are recorded in the
JSON summary. This normalization is limited to the new follow-up output;
all historical logs and corpus records remain byte-for-byte unchanged.

Each A5 case has zero cross-view or privacy violations but nine repeated
all-frontier invariant findings for its two votes and admit. All five
readers agree on the incorrect effective outcomes; no application is
recorded by the model. The original rejection/no-grant expectations fail:

| Target | Votes | Admit / Member grant | Full / minimal steps |
| --- | --- | --- | --- |
| Dana's invitation acceptance at 6 | 9, 10 effective | 11 effective / grant to Dana at 12 | 11 / 5 |
| Malformed ineffective apply at 9 | 10, 11 effective | 12 effective / grant to Dana at 13 | 12 / 6 |
| Standing reason at 9 | 10, 11 effective | 12 effective / grant to Alice at 13 | 12 / 4 |

Complete linked traces, their minima, observed findings and original
expected policy outcomes are in `corpus/club/a5-followup1/`. Every single
deletion of each minimum removes its unknown-application invariant
failure. A minimum can stop at one vote; the full test separately retains
the two votes, admission and grant. These are negative observations,
not successful policy tests or a repaired experiment.

All four direct historical corpus replays pass with their retained
package/manifest/mutation identities and expected readers/frontiers.
The follow-up checks every stored control id, matches the regenerated
minimum, checks every deletion and confirms the unmutated control. Source
or module changes that alter a retained mutant id now fail the test.
No new model fixes or kinds are counted.
